# Copy to .env and fill in. .env is gitignored.
# Only the providers you actually route to in models.yaml need a key.

MISTRAL_API_KEY=
GEMINI_API_KEY=

# Ollama runs locally and needs no key. Override only if not on the default port.
# OLLAMA_BASE_URL=http://localhost:11434/v1
